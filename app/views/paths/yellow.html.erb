<%= content_for :extra_footer do %>
  <link href="/assets/yellow.css" media="screen" rel="stylesheet">
<% end %>

<% if flash.any? %>
  <div class="flash">
    <div class="container">
      <h1 style="color:black"><%= flash[:notice]%></h1>
    </div>
  </div>
<%end%>

<ul id="nav-two">
  <ul class="yellow-boxes" id="buttons" style ="position:relative;margin: auto;width: 750px;">
    <%@path_array.each do |paths|%>
      <li>
        <a class="green box gas" onmouseover="playclip();" <a style="cursor: pointer" onclick="javascript: window.location = '<%=paths%>'" </a></a>
      </li>
    <%end%>
  <audio id="beep-two">
  <source src="/assets/hover.mp3" controls></source>
  <source src="/assets/click.ogg" controls></source>
  </audio>
  </ul>
</ul>

<script>$("#nav-two a")
  .each(function(i) {
    if (i != 0) {
      $("#beep-two")
        .clone()
        .attr("id", "beep-two" + i)
        .appendTo($(this).parent());
    }
    $(this).data("beeper", i);
  })
  .mouseenter(function() {
    $("#beep-two" + $(this).data("beeper"))[0].play();
  });
  $("#beep-two").attr("id", "beep-two0");
</script>
